syntax = "proto2";

package com.seagate.kinetic.proto;

message InternalValueRecord {
    required bytes version = 1;
    optional bytes value = 2;
    required bytes tag = 3;
    optional int32 algorithm = 4;
    optional bool badData = 5;
}
