#ifndef KINETIC_DEBUG_H_
#define KINETIC_DEBUG_H_
// For Engineering Debug, comment out the statement "#define KDEBUG" below
// and use DLOG(INFO) instead of LOG(INFO) for debugging.
// When compiling, all DLOG statements will not be complied if KDEBUG is defined.
#define KDEBUG

#endif  // KINETIC_DEBUG_H_
